<%= form_for @score, :remote => true, :html => { :class => 'form-horizontal' } do |f| %>
    <%= f.select :value, @score.names.map{ |c| [c, @score.names.index(c)] } %>
    <%= f.hidden_field :text_id %>
    <%= f.hidden_field :student_id %>
    <%= f.hidden_field :teacher_id %>
    <%= f.submit 'Save', :class => 'btn btn-primary' %>

    <%=
    #    link_to t('.cancel', :default => t("helpers.links.cancel")), score_path, :class => 'btn'
    %>


<% end %>
